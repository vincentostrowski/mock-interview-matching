const Countdown = ({ session }) => {
  //should display the session and count down to the start of the session
  //when it reaches 10 minutes before the session, it should display a button to join the session

  return (
    <div>
      <h1>Countdown</h1>
    </div>
  );
};

export default Countdown;
